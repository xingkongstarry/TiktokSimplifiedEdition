# TikTokSimplifiedEdition - 抖音消息列表简版 (Android)

本项目是仿照抖音消息页面的 Android 原生应用，基于 **MVVM 架构** 开发。实现了消息列表混排、实时消息模拟、本地持久化、搜索高亮、多类型消息展示以及弱网体验优化等功能。

## 📱 项目截图

| 消息列表(正常) | 消息列表(骨架屏) | 搜索与高亮 | 聊天详情(多类型) | 备注与置顶 |
|:---:|:---:|:---:|:---:|:---:|
| <img width="560" height="1162" alt="image" src="https://github.com/user-attachments/assets/1a54c011-85c1-46dc-a70b-6ee23a6efa88" /> | <img width="562" height="1190" alt="image" src="https://github.com/user-attachments/assets/1722b17c-457f-49d1-bef3-d5552aef019f" /> | <img width="554" height="1164" alt="image" src="https://github.com/user-attachments/assets/4e3dd46a-40d5-4ed5-86a5-d20f5d8d5bf9" /> | <img width="553" height="1177" alt="image" src="https://github.com/user-attachments/assets/abe670d2-adbc-487d-aabf-c3006aa77ee2" />
 | <img width="555" height="1162" alt="image" src="https://github.com/user-attachments/assets/9cd6ee06-b7bd-4727-a0e0-fd9c2f2f99d9" /> |

## 🛠 技术栈与架构

* **语言**: Java
* **架构**: MVVM (Model-View-ViewModel)
* **核心组件**:
    * `Jetpack ViewModel` & `LiveData`: 状态管理与数据驱动
    * `Room Database`: 本地 SQLite 数据库封装与 ORM
    * `RecyclerView`: 复杂列表展示
* **图片加载**: Glide
* **其他**: Handler (模拟消息推送), SpannableString (搜索高亮)

## ✨ 功能特性

### 基础功能
- [x] **消息列表**: 好友消息与系统消息混排，支持头像、未读角标、时间、内容预览。
- [x] **交互体验**: 支持下拉刷新 (`SwipeRefreshLayout`)、上滑加载更多。
- [x] **备注功能**: 可修改好友备注名，并持久化保存至本地数据库。
- [x] **数据持久化**: 基于 Room 实现，冷启动数据不丢失。

### 进阶功能
- [x] **消息中心模拟**: 后台定时器每隔 5-10s 随机插入新消息，列表自动刷新并置顶。
- [x] **多类型消息**: 支持 **纯文本**、**图片消息**、**运营卡片(带按钮)** 三种展示形态。
- [x] **时间格式化**: 实现“刚刚”、“xx分钟前”、“昨天”、“MM-dd”等仿抖音逻辑。
- [x] **搜索增强**: 支持昵称/内容搜索，并对命中关键词进行 **红色高亮** 显示。
- [x] **交互动画**: 列表点击进入详情页、返回页面的自定义转场动画。
- [x] **数据库迁移**: 实现了 Schema 升级 (Version 1 -> 2)，新增 `isPinned` (置顶) 字段，支持长按置顶。
- [x] **弱网/无网体验**: 封装 `Resource` 类，实现加载中骨架屏、超时错误提示及重试机制。

## 🧩 核心难点与解决方案

1. **多类型消息的高效渲染**
   - **难点**：聊天界面包含文本、图片、运营卡片等多种形态，逻辑复杂。
   - **解决**：利用 RecyclerView 的 `getItemViewType` 分发机制，配合多套 `ViewHolder` 和 XML 布局，实现了高度可扩展的消息展示框架。

2. **搜索关键词高亮**
   - **难点**：需要在不破坏原有文本样式的同时，对匹配字符进行变色。
   - **解决**：使用 `SpannableStringBuilder` 和 `ForegroundColorSpan`，通过正则/索引定位动态替换文本样式，实现了无损高亮。

3. **数据库无损迁移**
   - **难点**：在不删除用户原有聊天记录的前提下，增加“置顶”功能字段。
   - **解决**：编写标准的 Room `Migration` (1 -> 2) 脚本，执行 `ALTER TABLE` SQL 语句，确保了 Schema 升级的数据安全性。

## 📂 工程结构

```text
com.starry.tiktoksimplifiededition
├── data
│   ├── db          # Room Database, DAO, Converters, Migrations
│   ├── model       # Entity (Message, ChatMessage)
│   └── repository  # 数据仓库 (处理 Mock 数据与 DB 操作)
├── ui
│   ├── activity    # MainActivity, ChatActivity, RemarkActivity
│   ├── adapter     # MessageAdapter (多布局), ChatAdapter
│   └── viewmodel   # MainViewModel (业务逻辑)
└── utils           # TimeUtils (时间格式化), Resource (状态封装)
```

## 🚀 运行指南

1.  Clone 本仓库到本地。
2.  使用 Android Studio (推荐 Hedgehog 及以上版本) 打开项目。
3.  等待 Gradle Sync 完成。
4.  连接真机或模拟器 (建议 API 26+)。
5.  Run 'app'。

---
